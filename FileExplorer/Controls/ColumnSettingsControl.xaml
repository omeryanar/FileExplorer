<UserControl x:Class="FileExplorer.Controls.ColumnSettingsControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
			 xmlns:dx="http://schemas.devexpress.com/winfx/2008/xaml/core"
             xmlns:dxg="http://schemas.devexpress.com/winfx/2008/xaml/grid"
			 xmlns:dxgt="http://schemas.devexpress.com/winfx/2008/xaml/grid/themekeys"
			 xmlns:dxlc="http://schemas.devexpress.com/winfx/2008/xaml/layoutcontrol"
			 xmlns:dxmvvm="http://schemas.devexpress.com/winfx/2008/xaml/mvvm"
			 xmlns:helpers="clr-namespace:FileExplorer.Helpers"
			 xmlns:model="clr-namespace:FileExplorer.Model">

	<UserControl.Resources>
		<Style x:Key="{dxgt:ExtendedColumnChooserThemeKey ResourceKey=TreeListViewStyle}" TargetType="{x:Type dxg:TreeListView}">
			<Setter Property="AllowSorting" Value="False" />
			<Setter Property="ShowColumnHeaders" Value="False" />
			<Setter Property="ShowVerticalLines" Value="False" />
			<Setter Property="ShowHorizontalLines" Value="False" />
			<Setter Property="ShowIndicator" Value="False" />
			<Setter Property="TreeLineStyle" Value="None"/>
			<Setter Property="ShowRootIndent" Value="False"/>
			<Setter Property="ShowSearchPanelMode" Value="Never" />
			<Setter Property="AutoExpandAllNodes" Value="True" />
			<Setter Property="ExpandNodesOnFiltering" Value="True" />
			<Setter Property="ShowDragDropHint" Value="True" />
			<Setter Property="NavigationStyle" Value="None" />
			<Setter Property="VerticalScrollbarVisibility" Value="Auto"/>
			<Setter Property="RowMinHeight" Value="28" />
			<Setter Property="RowStyle">
				<Setter.Value>
					<Style TargetType="dxg:RowControl">
						<Setter Property="Margin" Value="{LWThemeValue '0,0,0,4', Win11='0' }" />
						<Style.Triggers>
							<Trigger Property="SelectionState" Value="Focused">
								<Setter Property="Background" Value="Transparent" />
							</Trigger>
						</Style.Triggers>
					</Style>
				</Setter.Value>
			</Setter>
		</Style>
	</UserControl.Resources>

	<DockPanel>
		<DockPanel DockPanel.Dock="Top" Margin="0,0,0,10">
			<TextBlock Text="{Binding AdjustColumnSettings, Source={StaticResource ResourceProvider}}" />
			<Button DockPanel.Dock="Right" HorizontalAlignment="Right" Content="{StaticResource IconRefresh}" Style="{StaticResource FlatButtonStyle}" 
				    Command="{DXCommand '@a($UserControl).LoadDefaultLayout()'}" ToolTip="{Binding LoadDefaultLayout, Source={StaticResource ResourceProvider}}" />
		</DockPanel>
		<Grid>
			<dxg:GridControl x:Name="FileListView" dx:DXSerializer.StoreLayoutMode="UI" dxg:GridSerializationOptions.AddNewColumns="False" dxg:GridSerializationOptions.RemoveOldColumns="False">
				<dxg:GridControl.ItemsSource>
					<model:FileModelCollection />
				</dxg:GridControl.ItemsSource>
				<dxmvvm:Interaction.Behaviors>
					<helpers:GridLayoutHelper LayoutChanged="GridLayoutHelper_LayoutChanged" />
				</dxmvvm:Interaction.Behaviors>
				<dxg:GridControl.View>
					<dxg:TableView ShowIndicator="False" AllowEditing="False" AllowColumnFiltering="False"
								   ShowVerticalLines="False" ShowHorizontalLines="False" ShowSearchPanelMode="Never"
						           HorizontalScrollbarVisibility="Visible" VerticalScrollbarVisibility="Hidden" />
				</dxg:GridControl.View>
				<dxg:GridControl.Columns>
					<dxg:GridColumn FieldName="Name" SortOrder="Ascending" Header="{Binding Name, Source={StaticResource ResourceProvider}}" />
					<dxg:GridColumn FieldName="Description" Header="{Binding Type, Source={StaticResource ResourceProvider}}" />
					<dxg:GridColumn FieldName="AllTags" Header="{Binding Tags, Source={StaticResource ResourceProvider}}" />
					<dxg:GridColumn FieldName="Extension" Header="{Binding Extension, Source={StaticResource ResourceProvider}}" Visible="False" />
					<dxg:GridColumn FieldName="ParentName" Header="{Binding ParentName, Source={StaticResource ResourceProvider}}" Visible="False" />
					<dxg:GridColumn FieldName="ParentPath" Header="{Binding ParentPath, Source={StaticResource ResourceProvider}}" Visible="False" />
					<dxg:GridColumn FieldName="FullPath" Header="{Binding Path, Source={StaticResource ResourceProvider}}" Visible="False" />
					<dxg:GridColumn FieldName="Size" Header="{Binding Size, Source={StaticResource ResourceProvider}}" />
					<dxg:GridColumn FieldName="DateCreated" Header="{Binding DateCreated, Source={StaticResource ResourceProvider}}" />
					<dxg:GridColumn FieldName="DateModified" Header="{Binding DateModified, Source={StaticResource ResourceProvider}}" />
					<dxg:GridColumn FieldName="DateAccessed" Header="{Binding DateAccessed, Source={StaticResource ResourceProvider}}" Visible="False" />
				</dxg:GridControl.Columns>
			</dxg:GridControl>
			<dxlc:GroupBox TitleVisibility="Collapsed" HorizontalAlignment="Right" VerticalAlignment="Bottom" Width="200" Margin="0,0,0,17" Height="150" CornerRadius="0" Background="Transparent">
				<dxg:ExtendedColumnChooserControl Owner="{Binding View, ElementName=FileListView}" FlowDirection="{Binding View.FlowDirection, ElementName=FileListView}" />
			</dxlc:GroupBox>
		</Grid>
	</DockPanel>
</UserControl>
